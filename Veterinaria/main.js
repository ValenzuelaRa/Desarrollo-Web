import Header from "./components/Header";
import Formulario from "./components/Formuliario";
import "./style.css"
const veterianria ={
  titulo: 'Veterinaria',
}
document.body.appendChild(Header(veterianria));
document.body.appendChild(Formulario());
